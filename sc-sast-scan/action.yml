name: 'Perform SAST scan'
description: 'Perform a SAST scan on ScanCentral SAST'
author: 'Fortify'
runs:
  using: composite
  steps:
    - uses: gendry-gh/github-action/internal/set-ssc-var-defaults@main
    - uses: gendry-gh/github-action/setup@main
      with:
        export-path: false
        fcli: action-default
        debricked-cli: ${{ env.DO_DEBRICKED_SCAN=='true' && 'action-default' || 'skip' }}
    - uses: gendry-gh/github-action/internal/ssc-login@main
    - uses: gendry-gh/github-action/package@main
    - uses: gendry-gh/github-action/internal/run-script@main
      with:
        script: sc-sast-and-debricked-scan.sh
      env: 
        DO_SC_SAST_SCAN: true
        GITHUB_TOKEN: ${{ github.token }}
    - if: env.DO_EXPORT == 'true'
      uses: gendry-gh/github-action/ssc-export@main
        
branding:
  icon: 'shield'
  color: 'blue'


